import {CarloadInvoiceItem} from './carload-invoice-item';

describe('CarloadInvoiceItem', () => {
  it('should create an instance', () => {
    expect(new CarloadInvoiceItem()).toBeTruthy();
  });
});
